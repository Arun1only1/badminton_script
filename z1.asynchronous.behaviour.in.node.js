// node => js runtime
// node => asynchronous
// synchronous => occurring at same time
// asynchronous => not occurring at same time
// node => single threaded
//  being single threaded, there is chance of blocking of code execution flow
// but node handles it using event loop
// node implements  non-blocking using event loop

// console.log("Hi");

// setTimeout(() => {
//   console.log("Timeout 1");
// }, 5000);

// setTimeout(() => {
//   console.log("Timeout 2");
// }, 1000);

// setTimeout(() => {
//   console.log("Timeout 3");
// }, 0);

// console.log("Hello");

// Stack => LIFO => Last In First Out
// Queue => FIFO => First In First Out

// Callstack full => maximum call stack size exceeded
